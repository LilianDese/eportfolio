---
description: Deploy E-Portfolio to GitHub
---

This workflow commits and pushes changes to the GitHub repository.

1. Stage changes
```bash
git add index.html style.css
```
2. Commit changes
```bash
git commit -m "Update portfolio structure and styles to match CAM M1 guidelines"
```
3. Push to GitHub
```bash
git push origin main
```
